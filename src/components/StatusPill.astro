---
interface Props {
  status: string;
  statusCs?: string;
}

const { status, statusCs = status } = Astro.props;
---

<span class="status-pill">
  <span class="status-dot" aria-hidden="true"></span>
  <span data-lang="en">{status}</span><span data-lang="cs">{statusCs}</span>
</span>
